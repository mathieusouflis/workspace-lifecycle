---
domain: operations
type: runbook
status: todo
owner:
last_exercised:
---

# TODO — Deployment runbook

<!--
TODO — this template has no real deployment target (no infra, no hosting
provider defined), so there is nothing real to document yet.

A Runbook goes stale by disuse, not by age — even a technically accurate
runbook is dangerous if no one has actually exercised it recently, because
the infrastructure around it can drift silently (renamed services, rotated
credentials, changed tooling) without the document itself becoming
"wrong" on paper. Once this is filled in, schedule a periodic dry run and
record it in `last_exercised` above, not just at write time.

Rename this file once filled in, dropping the `todo_` prefix.
-->

## When to use this runbook

<!-- The trigger: an alert firing, a scheduled deploy, a specific error condition. -->

## Prerequisites

<!-- Access/credentials/tools required before starting. -->

## Steps

1. <!-- ... -->

## Verification

<!-- How to confirm the procedure succeeded — not just "no errors", a concrete signal. -->

## Rollback

<!-- What to do if the procedure fails partway through. -->

## Last exercised

<!-- Date of the last real or drill run, and what (if anything) it caught. -->

## See also

- [operations/concept/ci-cd-pipeline](../concept/ci-cd-pipeline.md)
