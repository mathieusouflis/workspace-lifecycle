---
domain: product-code
type: decision-record
status: todo
owner:
supersedes:
---

# TODO — Title of the decision, as a short noun phrase

<!--
TODO — this is a skeleton, not a real decision. A template has no real
architectural choice to record yet.

When you have a real decision to document:
1. Fill in the sections below.
2. Rename this file, dropping the `todo_` prefix and adding a sequence
   number scoped to this domain's decisions/ folder: `0001-<slug>.md`.
3. Set `status:` to `proposed` while under discussion, then `accepted`
   once decided. Never edit an accepted decision after the fact — if it
   needs to change, write a new file that supersedes it (see below).

Numbering is per-domain (product-code/decisions/, organizational/decisions/,
etc. each have their own 0001, 0002... sequence) rather than one global
count, because each domain has a different owning team.
-->

## Context

<!-- What forces are at play — technical, business, team — that make this decision necessary? What alternatives were considered? -->

## Decision

<!-- The choice that was made, stated as a plain declarative sentence. -->

## Consequences

<!-- What becomes easier or harder as a result of this decision. Include the tradeoffs accepted, not just the benefits. -->

## Superseded by

<!-- Leave empty. If a later decision replaces this one, this decision's status becomes `superseded` and this section links to the file that replaces it. This file is never edited to reflect the new choice — immutability is the point. -->
