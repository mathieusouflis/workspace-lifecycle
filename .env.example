# Copy this file to .env and fill in the values.
# Never commit the .env file — it is listed in .gitignore.

# workspace-lifecycle is a local CLI with no network service, database, or
# auth of its own — none of the usual server/DB/auth variables apply. Add
# project-specific variables below as they're introduced (e.g. scan roots,
# staleness thresholds).
