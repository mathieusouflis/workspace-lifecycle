inputs:
  nixpkgs:
    url: github:cachix/devenv-nixpkgs/rolling

# terraform is BSL-licensed (unfree) — nixpkgs blocks unfree packages by
# default, so this needs to be explicit rather than a silent global default.
allowUnfree: true
